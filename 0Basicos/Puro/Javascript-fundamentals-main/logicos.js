// v e v -> v;
// v e f -> f  // operações com "e" basta uma afirmativa ser falsa para o resultado ser "f" (&&)
// f e ? -> f

// v ou ? -> v
// f ou v -> v  // operações com "ou" basta uma afirmativa ser verdadeira para o resultado ser "v" (||)
// f ou f -> f 

// v xor v -> f
// v xor f -> v  // com ou exclusivo obrigatoriamente as duas afirmativas precisam ser diferentes para ser "v"
// f xor v -> v
// f xor f -> f

// !v -> f
// !f -> v  // negação lógica transforma no oposto.